# docker-compose.safe.yml
#
# SafeClaw = OpenClaw + AEP safety proxy
# Run this instead of docker-compose.yml for safety and accountability.
#
# Usage:
#   cp .env.example .env  # add your API keys
#   docker compose -f docker-compose.yml -f docker-compose.safe.yml up
#
# Dashboard: http://localhost:8899/dashboard/
# Every LLM call: cost tracked, safety enforced, PASS/FLAG/BLOCK decisions.

services:
  # AEP safety proxy — sits between OpenClaw and the LLM API
  # Uses pre-built GHCR image (instant start, models pre-cached).
  # Falls back to local build if image not available.
  aep-proxy:
    image: ghcr.io/aceteam-ai/aep-proxy:latest
    build:
      context: .
      dockerfile_inline: |
        FROM python:3.12-slim
        RUN pip install --no-cache-dir "aceteam-aep[all]"
        EXPOSE 8899
        ENTRYPOINT ["aceteam-aep"]
        CMD ["proxy", "--port", "8899", "--host", "0.0.0.0"]
    ports:
      - "8899:8899"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8899/dashboard/')",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Override OpenClaw gateway to route LLM calls through AEP proxy.
  #
  # The sentinel default for OPENAI_API_KEY / ANTHROPIC_API_KEY matters:
  # OpenClaw refuses to dispatch LLM calls unless it has *some* key in env or
  # auth-profiles.json. The AEP proxy (v0.9+) substitutes its own managed key
  # before forwarding upstream, so the sentinel is a placeholder — never sent
  # to OpenAI/Anthropic. Users who prefer direct BYOK just set the real key
  # in .env; OpenClaw uses that verbatim and the proxy passes it through.
  openclaw-gateway:
    environment:
      OPENAI_BASE_URL: http://aep-proxy:8899/v1
      ANTHROPIC_BASE_URL: http://aep-proxy:8899/v1
      OPENAI_API_KEY: ${OPENAI_API_KEY:-aep-proxy-managed}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-aep-proxy-managed}
      AEP_ENTITY: ${AEP_ENTITY:-}
      AEP_CLASSIFICATION: ${AEP_CLASSIFICATION:-}
      AEP_TRACE_ID: ${AEP_TRACE_ID:-}
    depends_on:
      aep-proxy:
        condition: service_healthy
